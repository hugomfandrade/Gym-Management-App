<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="org.hugoandrade.gymapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".view.LoginActivity"
            android:screenOrientation="portrait"
            android:configChanges="orientation"
            android:windowSoftInputMode="adjustResize" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".view.SignUpActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".view.admin.AdminMainActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".view.admin.GymStaffListActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".view.admin.GymMemberListActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".view.admin.CreateStaffActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".view.staff.StaffMainActivity"
            android:screenOrientation="portrait"/>

        <activity
            android:name=".view.member.MemberMainActivity"
            android:screenOrientation="portrait"/>

        <service
            android:name=".model.service.MobileClientService"
            android:process=":azureService"
            android:enabled="true"/>

    </application>

</manifest>
